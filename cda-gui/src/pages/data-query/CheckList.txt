CheckList

Create branch with the custom table code ----
Jump back to default branch and remove all custom table code ----
Setup to use the simple groundwork-water table ---100%
Setup to use CWMSPlot (I think we have this) ----100%
Create the tab selection below the dropdowns to select plot and table (we might have this)
Setup office select dropdown that queries the offices endpoint for all DISTRICT offices 
Load timeseries from that office, config we have setup - load the config from BLOB???? ----Haven't made a blob yet but testing it with a config js
Setup download button for CSV and JSON-------1/2
Add datum toggle

CONVERT to using input with smart dropdown

TODO:
Add multi TS support
    - Sidebar of TS that have been selected
    - Allow for closing TS on the side or toggling their visibility (eyeball)
Make it so the guided mode lets you pick any of the params at any time (i.e. start with version if you want)
If only one option is left in a field, auto select that
Add Scroll bars to the table

Reach out to districts to review and critique
Asc/Desc table
Add SI/EN toggle